buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:0.13.+'
    }
}

repositories {
    mavenCentral()
}

ext {
    compileSdkVersion = "android-20"
    buildToolsVersion = "20.0.0"
}
